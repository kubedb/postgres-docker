apiVersion: kubedb.com/v1
kind: Postgres
metadata:
  name: demo-pg-partman
  namespace: demo
spec:
  version: "16.4-alpine-pg-partman"
  replicas: 3
  standbyMode: Hot
  storageType: Durable
  storage:
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: 2Gi
  deletionPolicy: WipeOut
