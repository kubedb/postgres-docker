apiVersion: kubedb.com/v1
kind: Postgres
metadata:
  name: demo-pg-temporal
  namespace: demo
spec:
  version: "16.8-debian-temporal-tables"
  replicas: 3
  standbyMode: Hot
  storageType: Durable
  storage:
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: 2Gi
  deletionPolicy: WipeOut
