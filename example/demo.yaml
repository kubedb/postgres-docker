apiVersion: kubedb.com/v1alpha2
kind: Postgres
metadata:
  name: demo-pgsqueeze
  namespace: default
spec:
  version: "11.11-pgsqueeze"
  replicas: 3
  standbyMode: Hot
  storageType: Durable
  storage:
    storageClassName: "standard"
    accessModes:
      - ReadWriteOnce
    resources:
      requests:
        storage: 1Gi
  terminationPolicy: WipeOut

